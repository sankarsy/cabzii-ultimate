import TravelLayoutClient from "../../components/mmt/TravelLayoutClient";

export const metadata = {
  title: "Call Driver in Chennai | Acting Driver Service | Cabzii",
  description:
    "Book a call driver in Chennai for your own car. Acting driver, outstation, airport, monthly, corporate and valet parking driver service on Cabzii."
};

export default function CallDriverLayout({ children }) {
  return <TravelLayoutClient>{children}</TravelLayoutClient>;
}
