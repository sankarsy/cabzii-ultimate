"use client";

import InactiveServicePage from "../../components/emt/InactiveServicePage";

export default function FlightsPage() {
  return (
    <InactiveServicePage
      title="Flight booking is paused"
      blurb="Flight booking is inactive on Cabzii for now. Book a cab, bus or holiday package for your trip instead."
    />
  );
}
